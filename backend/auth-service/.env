# SERVER CONFIG
PORT=8080
FRONTEND_URL=http://localhost:9090

# POSTGRESQL CONFIG
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mylaw_auth_db
DB_USER=postgres
DB_PASSWORD=your_local_password

# JWT SECRETS (We will use these in the next step)
JWT_SECRET=super_secret_enterprise_key_change_in_production
JWT_EXPIRES_IN=24h